using NFramework.Persistence.Abstractions.Pagination;

namespace {{ Service }}.Core.Application.Features.{{ Feature }}.Queries.List{{ Name }}s;

public record List{{ Name }}sQueryResult(PaginatedList<{{ Name }}Dto> Data);

public record {{ Name }}Dto(
    {{ IdType | default(value="Guid") }} Id{%- for prop in Properties %},
    {{ prop.type }}{% if prop.nullable %}?{% endif %} {{ prop.name }}
    {%- endfor %}
    {%- if EntityType == "auditable_entity" or EntityType == "soft_deletable_entity" %},
    DateTime CreatedAt,
    DateTime? UpdatedAt
    {%- endif %}
    {%- if EntityType == "soft_deletable_entity" %},
    bool IsDeleted,
    DateTime? DeletedAt
    {%- endif %}
);
