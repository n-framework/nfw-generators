using NFramework.Persistence.EFCore.Repositories;
using {{ Service }}.Core.Application.Contracts.Persistence;
using {{ Service }}.Core.Domain.Features.{{ Feature }}.Entities;
using {{ Service }}.Infrastructure.Persistence.Shared.Database.Contexts;

namespace {{ Service }}.Infrastructure.Persistence.Features.{{ Feature }}.Repositories;

public class {{ Name }}Repository : EFCoreRepository<{{ Name }}, {{ IdType | default(value="Guid") }}, BaseDbContext>, I{{ Name }}Repository
{
    public {{ Name }}Repository(BaseDbContext context) : base(context)
    {
    }
}
